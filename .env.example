# youVsBTC / VaultBench API host used by the dashboard.
# CORS on the API allows theweb3boy.com, www.theweb3boy.com and localhost:3000.
# Production host (do not point a local review at this unless you intend to).
NEXT_PUBLIC_VAULTBENCH_URL=https://api-production-e1b0.up.railway.app
# Local controlled dataset: pnpm dashboard:fixture (from the engine repo)
# NEXT_PUBLIC_VAULTBENCH_URL=http://localhost:8787
