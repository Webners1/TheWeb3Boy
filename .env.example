# Postgres connection string for the ingest/backfill jobs and drizzle-kit.
# Never commit a real value — CI reads this from repository secrets.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vaultbench
